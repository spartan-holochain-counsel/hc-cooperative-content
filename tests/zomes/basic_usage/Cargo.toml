[package]
name = "basic_usage"
version = "0.0.0"
edition = "2018"

[lib]
name = "basic_usage"
crate-type = ["cdylib", "rlib"]

[dependencies]
coop_content_types = { path = "../../../coop_content_types" }
serde = "1"
test_types = {path = "../../test_types"}
